# --- web ---------------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3

# --- storage -----------------------------------------------------------------
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
chromadb>=0.5.3

# --- llm (Groq: chat completions) --------------------------------------------
groq>=0.9.0

# --- embeddings --------------------------------------------------------------
# Primary provider is the Jina AI HTTP API, called through httpx (below).
# fastembed is the offline fallback: local ONNX, no PyTorch. It is also what
# the cross-encoder reranker uses, so keep it even when EMBEDDING_PROVIDER=jina.
fastembed>=0.4.0

# --- sparse retrieval --------------------------------------------------------
rank-bm25>=0.2.2

# --- document parsing --------------------------------------------------------
PyMuPDF>=1.24
python-docx>=1.1.2
tiktoken>=0.7.0

# --- http client (Jina embeddings + test client) -----------------------------
httpx>=0.27

# --- tooling -----------------------------------------------------------------
python-dotenv>=1.0.1
pytest>=8.2
